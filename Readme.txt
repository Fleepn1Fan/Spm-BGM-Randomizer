How to use:
-Extract Spm ISO in a folder called spm
-Put SpmBgmRandomizer next to the spm folder
-Open SpmBgmRandomizer
-Rebuild the ISO

Note that this mod is still in version 1.0, so it may crash.